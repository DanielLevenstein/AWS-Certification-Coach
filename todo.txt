- Keep two version of curated_training_data one commited to source control in config dir and one that is not in data/curated dir.
- When training data is copied from config to curated dir the files should either be versioned unless the two files are an exact match.
- Add a folder to data directory for screenshots and add a screenshot of user questions when feedback UI is used.
- Also unit test need to be converted into actual unit tests with a helper method living at the root directory to kick them off.